{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Headless Control",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/dev-tools/headless-local-control.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "python": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}
